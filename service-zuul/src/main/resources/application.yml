eureka:
  client:
    serviceUrl:
      defaultZone: http://test1:1111/eureka/
server:
  port: 12321
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-ribbon
    api-b:
      path: /api-b/**
      serviceId: service-feign